replicaCount: 1

image:
  repository: 54.209.36.3:5000/node-app
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

env:
  APP_ENV: dev
  APP_VERSION: "local"

resources: {}
